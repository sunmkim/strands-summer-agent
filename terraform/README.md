# Infrastructure-as-code

## Using Terraform

This all requires some cloud resources to be already deployed to AWS when launching. We will use Terraform to easily create and destroy these resources.

Run the following commands:
```
?> terraform init
?> terraform validate
?> terraform apply
```